[project]
name = "hof-card-data"
version = "0.1.0"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
hof = "src.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]


